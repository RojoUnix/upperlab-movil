import { EquipoEstadoPipe } from './equipo-estado.pipe';

describe('EquipoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
